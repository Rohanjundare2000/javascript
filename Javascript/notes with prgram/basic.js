//With the object model, JavaScript gets all the power it needs to create dynamic HTML:

// JavaScript can change all the HTML elements in the page
// JavaScript can change all the HTML attributes in the page
// JavaScript can change all the CSS styles in the page
// JavaScript can remove existing HTML elements and attributes
// JavaScript can add new HTML elements and attributes
// JavaScript can react to all existing HTML events in the page
// JavaScript can create new HTML events in the page

//..............................................................................................

//variable

//variable : It is the name of the memory location where we store a data 
// let a=10 here, a is the variable 
// there are two type of the variable : local variable and global variable 
// local variable: Those variable which stored inside the function block
function rohan()
{
    let a;
}
// global varible: Thosee variable which stored outside the function and inside class
let b;

//..................................................b.........b.........b.........b...b.........b......b..

//Datatype

// it id the type and size of the data that can be stored in the variable 
// two type of the datatype : primitive datatye and non primitive datatype 
// primitive datatype: number null boolean string undefined 
// non primitive datatype : Array and object 

//...............................................................................................

// Operator:

//operator: Operator is the symbol which is used to perform a specific task that is mathamatical or logical 
// Type of operator:
// 1) arithamatical: +,*,/ 
// 2) logical : &&, !!,~
// 3) comparision: <,> ,= ,<= ,>=,==,===
// 4) assignment operator: =
// 5) increment ++i i++ , decrement

//.....................................................................................................

//Statement

// if

let a= 10
if(a==10)
{
    console.log(a)
}

// if else
let c =10;
if(c==20)
{
    console.log(c)

}
else{
    console.log(c)
}

// if else if 
let d =10;
if(d==20)
{
    console.log(d)

}
else if(d>c) 
{
    console.log(d)
}
else{

}
//...............................................................

///switch statement 
// when u required mutiple output fro single input 
let z= 10; 
switch(z){

    case 1:
    console.log(a)

    case 10:
    console.log(b)
}
//...........................................................................





 
